{
  "name": "mce-monorepo",
  "type": "module",
  "private": true,
  "version": "0.1.3",
  "packageManager": "pnpm@10.19.0",
  "scripts": {
    "build": "pnpm -F mce -F @mce/* build",
    "build:preset": "pnpm -F mce build:preset",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "cli": "pnpm -F @mce/cli cli",
    "stub": "pnpm -F @mce/cli stub",
    "dev": "MODE=local pnpm -F playground dev",
    "publish": "pnpm -r publish --no-git-checks --access public",
    "version": "conventional-changelog -p angular -i CHANGELOG.md -s && git add CHANGELOG.md",
    "release": "bumpp package.json packages/*/package.json --commit \"release: v%s\" --push --all --tag"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^6.1.0",
    "@types/node": "^24.9.1",
    "bumpp": "^10.3.1",
    "conventional-changelog-cli": "^5.0.0",
    "eslint": "^9.38.0",
    "typescript": "^5.9.3",
    "vite": "^7.1.12",
    "vitest": "^4.0.3",
    "vue": "^3.5.22",
    "vue-tsc": "^3.1.2"
  }
}
